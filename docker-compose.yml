services:
  talos:
    build: .
    env_file: .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped

  # Optional: lokales Modell via Ollama (auskommentieren zum Aktivieren).
  # Dann in .env: TALOS_BASE_URL=http://ollama:11434/v1
  # ollama:
  #   image: ollama/ollama
  #   volumes:
  #     - ollama:/root/.ollama
  #   restart: unless-stopped

# volumes:
#   ollama:
